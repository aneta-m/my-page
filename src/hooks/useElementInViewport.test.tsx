import useElementInViewport from "./useElementInViewport";
import { screen, render } from "@testing-library/react";
import React, { useRef, useState } from "react";

test("calls inViewportCallback when element is in viewport", () => {});
